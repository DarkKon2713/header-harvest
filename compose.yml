services:
  headerharves:
    build: .
    container_name: headerharves
    restart: unless-stopped
    ports:
      - "9191:9191"
    environment:
      - PORT=9191
      - HEADLESS=false
      - MAX_TIMEOUT=60000
      - PROXY_URL=
    shm_size: "2gb"
    security_opt:
      - seccomp:unconfined
